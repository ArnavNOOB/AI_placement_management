/**
 * Next.js Configuration Options
 * Configured in ES modules format for the JavaScript version of the project.
 * 
 * @type {import('next').NextConfig}
 * @author Arnav Garg
 * @version 1.0.0
 */
const nextConfig = {
  reactStrictMode: true,
  // We can add other standard options if needed
};

export default nextConfig;
